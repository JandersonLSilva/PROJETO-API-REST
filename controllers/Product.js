const { sucess, fail } = require('../helpers/response');
const ProductDAO = require('../service/Product');

// PRODUCTS Controller.
module.exports = {
    product: async function() {
        
    }
};