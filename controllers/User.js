const { sucess, fail } = require('../helpers/response');
const UserDAO = require('../service/User');

// USERS Controller.
module.exports = {
    user: async () => {
        
    },
    login: async () => {
        
    },
    signup: async () => {
        
    }
};
